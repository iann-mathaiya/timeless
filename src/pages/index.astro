---
import { auth } from "../lib/auth";
import Logo from "../assets/logo.svg";
import RootLayout from "../layouts/RootLayout.astro";

const authDetails = await auth.api.getSession({
	headers: Astro.request.headers,
});

if (authDetails) {
	return Astro.redirect("/home");
}
---

<RootLayout>
	<section class="mx-auto w-full min-h-screen bg-gradient-to-br from-[#f8f9c7]/80 to-[#f0cd97]/80">
		<div class="pt-28 flex flex-col items-center justify-center">
			<Logo width={40} height={40} fill="currentColor" />
			<h1 class="mt-4 font-serif text-4xl font-medium">
				A space for your day dreams and memories
			</h1>
			<p class="mt-1 text-sm text-gray-600">
				Save anything you want - journal your day with a photo gallery
				built to record your memories
			</p>

			<div class="mt-4 sm:mt-6 flex items-center justify-center gap-3">
				<a href="/join" class="px-4 py-1.5 text-sm text-white font-medium bg-orange-600 hover:scale-110 rounded-full transition-all duration-300 ease-in-out">
					Join Now
				</a>
				<a href="/sign-in" class="px-4 py-1.5 text-sm font-medium text-orange-600 bg-orange-200/50 border border-orange-300  hover:scale-110 rounded-full transition-all duration-300 ease-in-out">
					Sign In
				</a>
			</div>

			<div>

			</div>
		</div>
	</section>
</RootLayout>
